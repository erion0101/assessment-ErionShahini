@inherits LayoutComponentBase

<PageTitle>Video Lab</PageTitle>

<div class="vl-app">
    <header class="vl-header">
        <div class="vl-header-inner">
            <a href="/user/index" class="vl-logo" aria-label="Video Lab Home">
                <span class="vl-logo-icon" aria-hidden="true">▶</span>
                <span class="vl-logo-text">Video Lab</span>
            </a>
            <button class="vl-nav-toggle" type="button" aria-label="Toggle menu" @onclick="ToggleNav">
                <span class="vl-nav-toggle-bar"></span>
                <span class="vl-nav-toggle-bar"></span>
                <span class="vl-nav-toggle-bar"></span>
            </button>
            <nav class="vl-nav @(navOpen ? "vl-nav-open" : null)">
                <NavMenu />
            </nav>
        </div>
    </header>
    <main class="vl-main">
        @Body
    </main>
</div>

@code {
    private bool navOpen;

    private void ToggleNav() => navOpen = !navOpen;
}
